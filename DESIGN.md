# 一揽子股票组合分析系统 — 设计文档

> 本文档描述 `D:\prjsrc\ashare\` 项目的整体架构、数据流、模块职责与关键设计决策。

---

## 1. 系统目标

构建一个**轻量级、零密钥、开箱即用**的 A 股 / 港股 / 美股量化研究工具，支持：

- 多市场股票数据获取（A股/港股/美股，日股/韩股/台湾股/欧股待扩展）
- 四层评分体系（宏观 / 政策 / 行业 / 个股）
- 组合构建（评分加权 + 风险平价）
- 周度 / 信号触发调仓
- 回测复盘 + 报告导出
- GUI 可视化 + 系统托盘 + 个股诊断器

盈利目标：创业板 + 20% Alpha（年化 25-35%），最大回撤 ≤ 25%。

---

## 2. 系统架构

```
┌─────────────────────────────────────────────┐
│               GUI Layer (tkinter)            │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐      │
│  │回测结果│ │实时监控│ │个股评分│ │新闻监控│      │
│  └──────┘ └──────┘ └──────┘ └──────┘      │
│  ┌──────┐ ┌──────┐ ┌──────────────────┐   │
│  │选股分析│ │个股诊断│ │  股票池管理      │   │
│  └──────┘ └──────┘ └──────────────────┘   │
└──────────────────┬──────────────────────────┘
                   │
┌──────────────────▼──────────────────────────┐
│          Engine Layer (portfolio_selector)   │
│  ┌────────┐ ┌────────┐ ┌────────┐          │
│  │数据获取 │ │评分引擎│ │组合构建│          │
│  └────────┘ └────────┘ └────────┘          │
│  ┌────────┐ ┌────────┐ ┌────────┐          │
│  │回测引擎│ │风控模块│ │报告生成│          │
│  └────────┘ └────────┘ └────────┘          │
└──────────────────┬──────────────────────────┘
                   │
┌──────────────────▼──────────────────────────┐
│            Data Layer                         │
│  ┌──────────────┐  ┌─────────────────────┐  │
│  │腾讯 ifzq API │  │ akshare 多源回退    │  │
│  │ (免费/无密钥) │  │ (财联社/东方财富等) │  │
│  └──────────────┘  └─────────────────────┘  │
│  ┌──────────────┐  ┌─────────────────────┐  │
│  │本地索引缓存   │  │个股价格缓存          │  │
│  │stock_index.csv│  │data/cache/prices/   │  │
│  └──────────────┘  └─────────────────────┘  │
└─────────────────────────────────────────────┘
```

---

## 3. 核心模块

### 3.1 portfolio_selector.py — 核心引擎

**职责**：数据获取、评分计算、组合构建、回测执行、报告生成。

**关键函数**：

| 函数 | 职责 |
|------|------|
| `fetch_price()` | 多市场日线数据获取（腾讯 fqkline / akshare） |
| `fetch_name()` | 股票名称查询（腾讯 qt.gtimg.cn） |
| `fetch_macro()` | 宏观指标获取（PMI/M2/CPI/PPI/Shibor/北向资金） |
| `fetch_news()` | 实时新闻获取（多源回退：财联社 → akshare） |
| `classify_channel()` | 技术形态识别（上升/下降/横盘/突破） |
| `analyze_holder_correlation()` | 股东数量与大单资金相关性分析 |
| `analyze_selection()` | 17类因子综合评分 |
| `classify_scenario()` | 综合场景判断（趋势+均线+双底双顶+支撑阻力） |
| `plot_diagnosis_charts()` | 诊断图表生成（四象限） |
| `backtest_portfolio()` | 组合回测引擎（评分加权/风险平价） |

### 3.2 portfolio_gui.py — GUI 应用

**职责**：用户界面、事件处理、实时监控、报告展示。

**标签页结构**：

| 标签 | 组件 | 功能 |
|------|------|------|
| 📈 回测结果 | ResultPanel | 净值曲线、绩效指标、交易记录 |
| 🔍 实时监控 | MonitorPanel | 数据获取日志（颜色高亮） |
| ⭐ 个股评分 | ScorePanel | 四层评分雷达图/表格 |
| 📰 新闻监控 | NewsPanel | 分类新闻展示 + 时间戳 |
| 🎯 选股分析 | SelectionPanel | 17类因子评分表格 |
| 🔬 个股诊断 | DiagnosisPanel | 8维度诊断 + 图表 + 双击弹窗 |

### 3.3 tray_icon.py — 系统托盘

**职责**：最小化到托盘、事件提示、双击恢复、右键菜单。

**实现方式**：优先使用 `pystray` 库，回退纯 `ctypes` Win32 实现。

**图标资源**：
- `data/cache/money.ico` — 窗口图标
- `data/cache/datanaly.ico` — 托盘默认图标
- `data/cache/target.ico` — 事件触发闪烁图标

---

## 4. 数据流

### 4.1 数据获取流程

```
用户操作 / 定时任务
        │
        ▼
  fetch_price(code, start, end)
        │
        ├─ A股/港股 ──► 腾讯 ifzq fqkline API
        │                    │
        │                    ▼
        │             解析 JSON → DataFrame
        │                    │
        ├─ 美股 ────────► akshare stock_us_daily
        │                    │
        ├─ 其他市场 ──────► 标记"暂不支持"
        │
        ▼
  log_callback ──► MonitorPanel 实时日志
        │
        ▼
  本地缓存 ──► data/cache/prices/{market}_{code}.csv
```

### 4.2 本地缓存三级更新策略

| 级别 | 触发条件 | 行为 |
|------|----------|------|
| 定期更新 | 每日定时 | 检查索引文件 mtime，过期则刷新 |
| 即时更新 | 数据缺失/过期 | 自动获取最新数据 |
| 触发式更新 | 用户操作/特定事件 | 手动刷新缓存 |

**索引文件**：`data/cache/stock_index.csv`（5543+ 只 A 股，含拼音首字母列）

---

## 5. 关键设计决策

### 5.1 数据源选择
- **优先腾讯 ifzq**：免费、无密钥、支持前复权、稳定可靠
- **akshare 作为回退**：财联社/东方财富/同花顺多源回退
- **弃用东方财富 push2**：返回不稳定/空数据

### 5.2 股票池整合
- `PortfolioApp.pool_list` 作为**唯一主数据源**
- 子面板（ScorePanel/SelectionPanel/DiagnosisPanel）通过 `refresh_pool()` 统一刷新
- 避免副本不同步风险

### 5.3 线程安全 UI 更新
- 后台线程不直接操作 tkinter 控件
- 统一使用 `self.winfo_toplevel().after(0, callback)` 调度 UI 更新

### 5.4 托盘实现
- 放弃 `pywin32`（pip 安装成功但 import 失败）
- 优先 `pystray` 库，回退纯 `ctypes` Win32 实现
- 直接使用 `root.winfo_id()` 作为托盘 HWND，避免创建隐藏窗口失败

### 5.5 零额外依赖策略
- 所有新功能集成到现有 tkinter GUI
- 不引入 `customtkinter`、`PyQt` 等重型框架
- 图表生成使用 `matplotlib` + `PIL`

---

## 6. 股票搜索逻辑

```
输入文本
    │
    ├─ 纯数字 ──► 股票代码匹配（支持前缀 sh./sz./hk.）
    │
    ├─ 含中文 ──► 中文名称精确匹配 → 拼音首字母匹配
    │
    ├─ 纯字母 ──► 拼音首字母非连续缩写匹配（如 gmt → GZMT）
    │
    ▼
返回 [{code, name, market}, ...]
```

**拼音映射表**：覆盖 200+ 常见汉字，支持非连续缩写（如 `gmt` 匹配贵州茅台 `GZMT`）。

---

## 7. 诊断器四象限图表

```
┌─────────────────────┬─────────────────────┐
│  ① 价格走势          │  ② 股东数量          │
│  折线图              │  折线图              │
│  收盘价 + 高低区间    │  股东户数变化         │
├─────────────────────┼─────────────────────┤
│  ③ 大单净流入        │  ④ 筹码集中度        │
│  柱状图              │  折线图              │
│  红涨绿跌 + 零轴      │  90%/70% 集中度      │
└─────────────────────┴─────────────────────┘
```

**双击弹窗放大**：支持 `ESC` 关闭，自适应屏幕大小。

---

## 8. 技术栈

| 组件 | 技术 |
|------|------|
| GUI 框架 | tkinter (标准库) |
| 数据处理 | pandas, numpy |
| 图表绘制 | matplotlib, Pillow |
| 数据源 | akshare, requests |
| 网络请求 | requests (带重试 + 超时) |
| 系统托盘 | pystray (优先) / ctypes (回退) |
| 缓存 | CSV 文件 (pandas to_csv/read_csv) |

---

## 9. 目录结构

```
D:\prjsrc\ashare\
├── portfolio_selector.py    # 核心引擎 (2200+ 行)
├── portfolio_gui.py          # GUI 应用 (1200+ 行)
├── tray_icon.py              # 系统托盘模块 (400+ 行)
├── quant.py                  # CLI 单股回测入口
├── README.md                 # 项目说明
├── CHANGELOG.md              # 变更日志
├── DESIGN.md                 # 本文件：设计文档
├── .hermes/
│   └── plans/                # Hermes 整合优化计划
├── data/
│   └── cache/
│       ├── stock_index.csv   # 5543+ 只股票索引
│       ├── prices/           # 个股价格缓存
│       ├── money.ico         # 窗口图标
│       ├── datanaly.ico      # 托盘默认图标
│       └── target.ico        # 事件闪烁图标
├── portfolio_report_latest.md
├── portfolio_equity_latest.csv
├── portfolio_trades_latest.csv
└── portfolio_chart_latest.png
```

---

## 10. 性能与优化

| 优化项 | 实现方式 |
|--------|----------|
| 数据缓存 | 本地 CSV 缓存，避免重复请求 |
| 线程安全 | `after()` 调度 UI 更新 |
| 网络超时 | 单次请求 15s 超时，3 次重试 |
| 日志采样 | 仅记录关键阶段（下载/完成/失败/跳过） |
| 图表优化 | `bbox_inches="tight"` + `dpi=150` |

---

## 11. 已知限制

1. **北交所 (bj)**：akshare 超时，腾讯返回空列表，当前返回空数据
2. **日股/韩股/台湾股/欧股**：数据源暂不支持，标记为"暂不支持"
3. **财联社 API**：偶发失效，已实现多源回退机制
4. **Hermes 后台启动**：后台 shell 无桌面交互，需用户直接双击运行

---

## 12. 未来扩展

- [ ] 添加日股/韩股/台湾股/欧股数据源
- [ ] 修复北交所数据获取
- [ ] 支持美股实时行情
- [ ] 增加更多技术指标（KDJ/BOLL/OBV）
- [ ] 添加投资组合风险平价权重自动优化
- [ ] 支持导出 Excel 格式报告
- [ ] 增加邮件/微信推送功能

---

*文档版本：v1.0 — 2026-08-14*
